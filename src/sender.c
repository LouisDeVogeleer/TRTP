#include <stdlib.h>
#include <stdio.h>

int main(int argc, char *argv[]){
	int op;
	int isErr;                /*  si vaut 1, ecrit les messages d'erreur sur le fichier errFile*/
	int isSTDout
	char * errFile;
	int isSource;         /*  si vaut 1, le payload vient de sourceFile. si vaut 0, le payload vient de STDIN*/
	char * sourceFile;
	char * payload;
	while( (op = getopt(argc, argv, "")) != -1){
		
	}
	
}